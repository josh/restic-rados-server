tail-logs
create-pool

sha256 blob-data HASH

exec restic-rados-server --pool=$RESTIC_RADOS_SERVER_POOL':config,,keys,locks,snapshots,data,index,' --listen $WORK/restic.sock &server&
wait4socket $WORK/restic.sock

exec curl --silent --unix-socket $WORK/restic.sock --request POST 'http://localhost/?create=true'
exec curl --silent --write-out '%{http_code}' --unix-socket $WORK/restic.sock --request POST --header 'Content-Type: application/octet-stream' --data-binary @blob-data 'http://localhost/data/'$HASH --output /dev/null
stdout '200'

exec rados --pool $RESTIC_RADOS_SERVER_POOL ls
stdout '^data/'$HASH'$'

kill server

-- blob-data --
pool type separators
